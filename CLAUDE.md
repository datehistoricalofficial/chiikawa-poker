# 吉依卡娃德州撲克 🌸

A browser-based Texas Hold'em poker game featuring Chiikawa characters. Open `index.html` directly in a browser — no build step required.

## macOS 完整磁碟取用權限設定

若在 macOS 上執行 Claude Code CLI 時遇到磁碟存取問題，請依以下步驟授予完整磁碟取用權限：

1. 打開 **系統設定 → 隱私權與安全性 → 完整磁碟取用權限**
2. 按 **＋** 號
3. 在檔案選擇視窗，按 **Shift + Command + G**（跳出「前往路徑」框）
4. 貼上以下路徑並按「打開」：

```
/usr/local/bin/claude
```

5. 確認開關為藍色 ✅

若找不到上述路徑，可改為加入：

```
/bin/bash
```
